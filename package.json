{
  "name": "node-js-mvc",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "make-seeder": "npx sequelize-cli seed:generate --name test",
    "test": "echo \"Error: no test specified\" && exit 1",
    "insert-seeder": "npx sequelize-cli db:seed:all",
    "rollback-seeder": "npx sequelize-cli db:seed:undo:all",
    "rollback": "npx sequelize-cli db:migrate:undo",
    "migrate": "npx sequelize-cli db:migrate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.8.0",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.28.0"
  },
  "devDependencies": {
    "sequelize-cli": "^6.5.2"
  }
}
